export interface Station {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
}

export interface StationResponse {
  id: string;
  name: string;
  latitude: number;
  longitude: number;
}
